# secrets / local config
.env
config.yaml
secrets/
*.key
*.pem

# generated work logs (선택: 커밋하고 싶으면 이 줄 삭제)
worklogs/*.md

# python
__pycache__/
*.pyc
.venv/
venv/
.uv/
*.egg-info/
.pytest_cache/

# PyInstaller 빌드 산출물
dist/
build/
*.spec

# 머신 종속(프리뷰 런처 등)
.claude/

# 버전별 exe 보관 폴더 — 바이너리는 git 에 안 올림(용량). 폴더/README 는 추적.
releases/*.exe
