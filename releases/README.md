# releases

단일 실행파일(.exe) 보관 폴더.

- **`worklog.exe`** = **항상 최신 버전** (빌드할 때마다 최신 빌드로 덮어씀). 평소엔 이걸 실행하면 된다.
- `worklog-<버전>.exe` = 버전별 보관본 (예: `worklog-0.1.2.exe`). 특정 버전으로 되돌릴 때.
- `build_exe.bat` 를 실행하면 빌드 후 `worklog-<버전>.exe`(버전별) 와 `worklog.exe`(최신) 둘 다에 **자동 복사**된다.
- `.exe` 바이너리는 용량 때문에 git 에 커밋하지 않는다(`.gitignore` 에서 `releases/*.exe` 제외).
  원격 배포는 태그(`vX.Y.Z`)에 맞춰 **GitHub Releases** 에 첨부하는 방식을 권장.

## 버전 목록
- `worklog-0.1.16.exe` — v0.1.16 (**세션 질답 기반 요약 전면 전환**[시간대별 흐름 + 프로젝트별, 무거운 날 map-reduce] · **ActivityWatch 완전 제거** · **전체 코드리뷰 개선 22건**[map-reduce 견고화·Obsidian 덮어쓰기 보호·Notion 변환·자동업데이트 정리·런처 강화 등])
- `worklog-0.1.15.exe` — v0.1.15 (0.1.14 의 apply→finalize + 확인 알림 흐름을 0.1.14→0.1.15 실제 업데이트로 검증하기 위한 타깃. 코드는 0.1.14 와 동일)
- `worklog-0.1.14.exe` — v0.1.14 (**자동 재실행 최종 철회 + 닫기 전 확인 알림** — 리서치 결과 onefile 자가추출 특성상 교체 직후 자동 재실행은 백신 스캔 레이스로 'DLL 로드 에러'를 확실히 못 피함이 확인됨. '교체는 확실히, 재실행은 사용자가'로 확정하되, 다운로드 완료 후 '확인 누르면 닫고 적용' 알림을 띄워 갑작스런 종료 제거. apply→finalize 2단계. ※근본 해결책 onedir 은 별도 과제로 보류)
- `worklog-0.1.13.exe` — v0.1.13 (0.1.12 의 자동 재실행을 0.1.12→0.1.13 업데이트로 실증하기 위한 타깃. 코드는 0.1.12 와 동일)
- `worklog-0.1.12.exe` — v0.1.12 (**자동 재실행 복원** — 교체 후 settle delay[백신 스캔 시간]를 두고 배치가 새 exe 를 자동 실행. 과거 실패 3원인[AV 레이스·스테이션 상실·Job Object] 각각 대응. best-effort라 실패 시 수동 실행 폴백. 교체 대기도 단축)
- `worklog-0.1.11.exe` — v0.1.11 (유지보수 릴리스 — 기능 변경 없음. 0.1.10의 'ping/cmd 창 안 뜸' 수정을 0.1.10→0.1.11 업데이트로 검증하기 위한 타깃 버전)
- `worklog-0.1.10.exe` — v0.1.10 (**자동 업데이트 시 ping/cmd 창 깜빡임 제거** — 헬퍼 배치를 DETACHED_PROCESS 대신 CREATE_NO_WINDOW[숨김 콘솔]로 띄워 내부 ping/move 창이 안 뜨게. ※ 이 수정은 0.1.10부터 나가는 업데이트에 적용됨)
- `worklog-0.1.9.exe` — v0.1.9 (**터미널 깜빡임 제거**[claude.CMD 셔임에 STARTUPINFO/SW_HIDE 적용]·**자동 업데이트 DLL 로드 에러 방지**[교체 후 레이스 유발 자동 재실행 제거]·**누출된 onefile 임시폴더 시작 시 자동 청소**)
- `worklog-0.1.8.exe` — v0.1.8 (유지보수 릴리스 — 기능 변경 없음. 0.1.7→0.1.8 자동 업데이트 검증용 버전 상향)
- `worklog-0.1.7.exe` — v0.1.7 (자동 업데이트 정리 — 다운로드·교체는 확실히 동작, 재시작은 best-effort라 "다시 실행" 안내로 정직화)
- `worklog-0.1.6.exe` — v0.1.6 (코드 리뷰 결함 7건 수정 — **자동 업데이트 한글 경로 버그픽스**[중요]·스캔루트 미마운트 보존·NaverWorks 페이지네이션·설정 원자적 저장·요약 중 저장잠금 등)
- `worklog-0.1.5.exe` — v0.1.5 (자동 업데이트 재시작 버그픽스 — 0.1.5부터 '업데이트하기'가 실제로 교체·재시작까지 동작)
- `worklog-0.1.4.exe` — v0.1.4 (설정 경로칸 네이티브 '찾아보기' 버튼[저장 폴더·Vault 폴더 선택, Private Key 파일 선택])
- `worklog-0.1.3.exe` — v0.1.3 (앱 내 **자동 업데이트**: GitHub Releases 확인 → '업데이트하기' → exe 통짜 교체·재시작)
- `worklog-0.1.2.exe` — v0.1.2 (생성 시 화면 전환[수집 중 → 조사 데이터 먼저 표시 + 문서 영역만 요약 중]·물리 드라이브만 선택(가상/클라우드 제외)+볼륨 레이블 표시·설정 문구 정리)
- `worklog-0.1.1.exe` — v0.1.1 (설정 UI 개편[좌측 탭·스크롤 잠금·버전/경로]·수집 소스 단순화[스캔 범위 on/off, 물리 드라이브만 선택]·수집 병렬화·AI 요약 대기 표시 버그픽스)
- `worklog-0.1.0.exe` — v0.1.0 (수집·시간대별 요약·지표/타임라인·Markdown/Obsidian/Notion·데스크톱 앱·트레이·단일 인스턴스)
